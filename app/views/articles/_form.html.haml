= link_to "Cancel", @article, :id => "cancel_editing"
= form_for @article do |f|
  .fields
    = f.label :title
    = f.text_field :title
  .fields
    = f.label :body
    = f.text_area :body
  .fields
    = f.submit
