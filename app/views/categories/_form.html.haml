.resource_links
  = link_to "Cancel", categories_path, :id => "cancel_editing"
= form_for @category do |f|
  .fields
    = f.label :title
    = f.text_field :title
  .fields
    = f.label :description
    = f.text_area :description
  .fields
    = f.submit
