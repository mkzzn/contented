%div[comment]
  .body= comment.body
  .resource_links
    - if can? :delete, comment
      = button_to "Delete", { :action => "destroy", :controller => "comments", :id => comment.id }, :method => :delete , :class => "destroy delete"
