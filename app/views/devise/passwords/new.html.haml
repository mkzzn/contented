%h2 Forgot your password?
= form_for(resource,
  :as => resource_name,
  :url => password_path(resource_name),
  :html => { :method => :post, :class => "auth"}) do |f|
  = devise_error_messages!
  .fields
    = f.label :email
    = f.text_field :email
  .fields= f.submit "Send me reset password instructions"
= render :partial => "devise/shared/links"
